# Real Estate Contact

A self-contained contact page built with HTML, CSS, and vanilla JavaScript. It has no build dependencies, backend, browser storage, remote assets, or network integrations.

## Run locally

Open `index.html` directly in a browser, or serve this directory with any static file server. The default `/` route contains the complete app; static hosts should route unknown paths to `index.html` if fallback behavior is desired.
